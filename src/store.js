import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    // 定义你的状态
  },
  mutations: {
    // 定义你的突变
  },
  actions: {
    // 定义你的动作
  },
  getters: {
    // 定义你的获取器
  }
});